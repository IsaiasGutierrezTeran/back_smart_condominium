# Variables de entorno para producción en AWS
# ==============================================

# Django Settings
DEBUG=False
SECRET_KEY=tu-clave-secreta-super-segura-aqui-cambiar-por-una-real
DJANGO_SETTINGS_MODULE=smart_condominium.settings.production

# Database Configuration
DB_NAME=condominiobd
DB_USER=postgres
DB_PASSWORD=CondominioDB2024!
DB_HOST=db
DB_PORT=5432
DB_SSLMODE=prefer

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Email Configuration (opcional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=admin@smartcondo.com
EMAIL_HOST_PASSWORD=tu-password-de-app-gmail
EMAIL_USE_TLS=True

# Security
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
CORS_ALLOWED_ORIGINS=http://localhost,https://localhost

# Domain
DOMAIN=localhost